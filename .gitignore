# Per-user runtime state — NEVER commit
params.sh
*.local.sh

# Private keys / secrets (defensive; keys live in ~/.ssh, not here)
# covers both spellings incl. the tool's own defaults luffy-arm-key / luffy-arm-admin-key
*_key
*_key.pub
*-key
*-key.pub
id_ed25519*
id_rsa*
*.pem
.netrc

# OS / editor cruft
.DS_Store
*.swp
*~

# Agent runtime state + stray generated images (must never land in the published skill)
.omc/
ChatGPT Image*
